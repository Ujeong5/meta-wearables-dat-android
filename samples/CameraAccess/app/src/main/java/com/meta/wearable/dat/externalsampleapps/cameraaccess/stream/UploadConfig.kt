package com.meta.wearable.dat.externalsampleapps.cameraaccess.stream

/**
 * 데스크톱 FastAPI 서버 주소다.
 *
 * 반드시 아래 IP를 데스크톱의 IPv4 주소로 바꾼다.
 * 휴대폰과 데스크톱은 같은 Wi-Fi에 연결되어 있어야 한다.
 */
object UploadConfig {
  const val SERVER_BASE_URL = "http://192.168.0.24:8000"
}
